# ---- 运行时产物 ----
logs/
*.log
hosts-backup/

# ---- 编译产物（可由 net-warp-bridge/main.go 复现）----
# go build -trimpath -ldflags "-s -w -H=windowsgui" -o net-warp-bridge.exe .
net-warp-bridge.exe
net-warp-bridge/net-warp-bridge.exe

# ---- AI 会话工作区（本地协作记忆，不属于交付物）----
.workbuddy/

# ---- 内部状态与本地工具链（开源仓库不含）----
HANDOFF.md
_*.txt

# 环境变量（不入库）
.env
.env.local

# ---- 测试再生输出 ----
tools/common-test.txt
tools/common-test-work/

# 工具 / 编辑器产出的备份文件（含提交门升级备份 pre-commit.bak）
*.bak
